version: 2
sources:
  - name: shop
    tables:
      - name: orders
        columns:
          - name: order_id
            data_type: bigint
          - name: amount_usd
            data_type: double
models:
  - name: stg_orders
    columns:
      - name: order_id
        tests: [not_null]
